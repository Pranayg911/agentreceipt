{
  "name": "agentreceipt",
  "version": "0.1.0",
  "description": "The signed proof your AI coding agent actually did the work. Drop a Claude Code / Cursor session in and get a Trust Receipt — a score, an archetype, and cited receipts cross-checking every claim ('all tests pass', 'bug fixed') against the session's own tool-call record. Lies caught. ed25519-signed, publicly verifiable.",
  "type": "module",
  "license": "MIT",
  "keywords": ["ai", "agents", "claude-code", "cursor", "coding-agent", "trust", "eval", "ed25519", "verifiable", "transcript"],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": { "agentreceipt": "./dist/cli.js" },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.2",
    "@types/node": "^22.10.5"
  }
}
